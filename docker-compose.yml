version: "3.9"

services:
  superdoc:
    image: harbourenterprises/superdoc:latest
    container_name: superdoc
    restart: unless-stopped
    ports:
      - "4100:4100"
    environment:
      - NODE_ENV=development
      - SUPERDOC_PORT=4100
      # Add any required env once docs confirm (e.g., license, storage paths)
    # volumes:
    #   - ./data/working/superdoc:/data


